
public class App {
    public static void main(String[] args) throws InterruptedException { 

       // Remove comments run examples
       // new GuessingGame(); //Small game in java console practicing basic statements
       // new MyFrame(); //Menu for a fighting game
       // new Border(); //Border frame
       // new Layout(); //Flow layout
       // new Grid(); //Grid layout
       // new Layered(); //Layered layout 
       // new OpenPage(); //New windows and buttons
       // new Dialogue();  //Dialogue / JOptions 
       // new Text(); //Text and Submit 
       // new checkBoxes(); //checkboxes and radiobuttons
       // new cBox(); //Combo boxes
       // new Slide(); //Slider bar
       // new Progress(); //Progress bar
       // new Menu(); //Menubar with file, edit and help dropdown's
       // new keylisten(); //KeyListeners; Example was with WASD movement.
       // new mouselisten(); //MouseListener
       // new dnd();  //Drag a picture around and drop it in new positions
       // new keybind(); //Keybinds move a box around the screen
       // new graphic(); //Drawing polygons and other shapes with graphics
       // new animation(); //Animation moving graphics ( Perfect Logo hitting corner )
       // new generic(); //Generic classes


       /*  -----Threads-----

         System.out.println(Thread.activeCount());
         Thread.currentThread().setName("Main");
         System.out.println(Thread.currentThread().getName());
         for(int i =3;i>0;i--) {
         System.out.println(i);
         Thread.sleep(1000);
         }
         System.out.println("You are done!");
       

         MyThread thread2 = new MyThread();
         thread2.setDaemon(true); // daemon is a low priority thread to run background or garbage collection
       
         thread2.start(); // Have to start thread
         System.out.println(thread2.isAlive()); 

        
         MyThread thread1 = new MyThread();  // this way lets you use implements but not extends
         //thread1.setDaemon(true); // will terminate if user terminates or error
         thread1.start();
         thread1.join(3000);  // Waits until its done to start the next threat ( with a timer in millis )
         myRun run1 = new myRun();
         Thread thread3 = new Thread(run1); // this way lets you use extends  but not implements
         //thread3.setDaemon(true); // will terminate if user terminates or error
         thread3.start(); */

    }

}
